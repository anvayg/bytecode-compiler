#include <vector>
#include "instruction.h"

namespace interpreter {
    using Expression = std::vector<ArgType>;

    std::vector<Instruction> compile(Expression) {
        std::vector<Instruction> ins;

        return ins;
    }
}